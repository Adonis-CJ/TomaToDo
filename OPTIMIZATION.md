# TomaTodo v1.1 优化方案

> 基于 v1.0 实际交付情况（11 个提交，PRD F1–F8 全部跑通，已在华为 MatePad 真机验证）的下一版本规划。
> 关联文档：`PRD.md`（产品需求）、`ENV.md`（环境配置）。

---

## 1. 背景与现状

v1.0 完成了从 0 到 1：番茄钟（前台服务 + 悬浮窗）、Todo 看板、学科标签、知识卡片（含拍照）、
遗忘曲线复习、学习统计、JSON 备份、设置。所有模块均可用，但以代码走查的视角看，
存在一部分「PRD 有要求但实现未闭环」的欠账，以及真机使用中暴露的体验短板。

本期优化三条主线：

1. **UI 升级（墨·纸 2.0）**：把设计系统从「能用」打磨到「精致」。
2. **卡片知识管理升级（本期重点）**：分科目管理 + 专属卡片撰写页。
3. **功能闭环补全**：修掉 v1.0 的技术欠账，让铃声/任务关联/通知等真正生效。

---

## 2. 已知问题与技术债清单（代码走查结果）

> 本清单为 v1.0 代码的实际欠账，是本期「功能闭环」的依据。

| # | 问题 | 位置 | 影响 |
|---|---|---|---|
| 1 | `ringtoneId` / `volume` 已存储但**未生效**，完成提示只播系统默认音 | `TimerService.playCompletion()` | 设置页铃声/音量形同虚设 |
| 2 | `task.pomodoroCount` 从未累加（`incrementPomodoro` 无调用方） | `TimerService.recordFocusSession()` | PRD F1 验收项未闭环 |
| 3 | 番茄钟无法关联任务（`taskId` 恒为 null），看板卡片无「开始番茄」入口 | `TimerViewModel.start()` / `BoardScreen` | PRD F1/F2 联动缺失 |
| 4 | 备份只导出 `cardImages` **数据库记录**，不含图片**文件** | `BackupManager` | 换机恢复后缩略图全部失效 |
| 5 | `POST_NOTIFICATIONS` 运行时权限未申请 | `AndroidManifest` / 启动流程 | Android 13+ 前台通知不显示 |
| 6 | 悬浮窗无权限引导（无权限时静默不显示）；原生 View 样式与主题脱节；点击主体不返回应用 | `FloatingWindowManager` | PRD §6.7 验收项未满足 |
| 7 | `enableEdgeToEdge` 后未处理 WindowInsets，内容可能顶入状态栏 | 各 Screen 根布局 | 顶栏贴边、被状态栏遮挡风险 |
| 8 | 字体未按 PRD 落地：`FontFamily.Serif/Monospace` 为系统回退，非 Noto Serif SC / IBM Plex Mono | `Type.kt` | 「墨·纸」书卷气打折扣；倒计时数字宽度可能跳动 |
| 9 | 复习 due 查询的 `now` 固定在 ViewModel 创建时刻，跨天不刷新 | `ReviewViewModel` | 长开应用时「今日待复习」漏卡 |
| 10 | 番茄通知随 200ms tick 刷新（5Hz） | `TimerService` | 通知频繁重建，浪费电 |
| 11 | `PomodoroSession` 无 `subjectId`，科目时间分布无从统计 | 数据模型 | PRD F6 科目分布不可实现 |
| 12 | 任务不可编辑；看板无日期切换、无科目筛选/分组 | `BoardScreen` | PRD F2 功能点未实现 |
| 13 | 卡片不可编辑、保存后图片不可删、无全屏预览/缩放 | `CardsScreen` | PRD F4 验收项未满足 |

---

## 3. UI 优化设计（墨·纸 2.0）

### 3.1 设计令牌升级

| 项 | 现状 | 优化 |
|---|---|---|
| 标题字体 | 系统 Serif 回退 | 引入 **Noto Serif SC**（assets + 子集化，仅常用字重），保证书卷气跨设备一致 |
| 倒计时字体 | 系统 Monospace | 引入 **IBM Plex Mono**（`FontVariant.tabularNums`），数字宽度绝对稳定 |
| 卡片层次 | 仅色块 | 纸卡 `surface` + 1dp hairline 描边（`outlineVariant`）+ 低海拔阴影，更「纸」 |
| 阶段语义色 | 单一朱砂 | 专注=朱砂 `#B4553A`，短休=松绿 `#4A6B5D`，长休=赭黄 `#A8893C`，进度环随阶段变色 |
| 背景 | 纯色 | 可选极淡纸张纤维纹理（`drawBehind` 噪点，低透明度；注意过度绘制与性能，默认关） |

### 3.2 布局与自适应

- **WindowSizeClass 双形态**：横屏/宽屏保持 Navigation Rail；竖屏/窄屏自动切换 **Bottom Navigation Bar**（平板分屏、折叠态场景）。
- **WindowInsets 全面接入**：根布局 `safeDrawingPadding()`，顶栏避开状态栏、底部避开手势条。
- **页面切换动效**：`AnimatedContent` 淡入 + 8dp 位移（150ms），替代瞬切。

### 3.3 组件与动效

- **卡片 3D 翻转**：`rotateY` + `AnimatedContent`，正反面翻转查看答案（替代当前瞬时切换文本）。
- **进度环**：`SweepGradient` 渐变描边 + 阶段变色 + 最后 60s 呼吸脉冲。
- **列表入场**：看板/卡片网格 stagger（每项延迟 30ms，仅首屏）。
- **空状态插画**：统一线性 SVG 风格插画（空看板/空卡片/无复习），替代纯文字。
- **触感**：完成番茄、翻转卡片等关键操作配 `HapticFeedback`（遵守「静音+仅震动」）。

### 3.4 各屏 UI 改造点

| 屏幕 | 改造点 |
|---|---|
| 看板 | 列头改为「色条 + 标题 + 计数」；卡片增加番茄进度点（●●○）；拖拽换列动画 |
| 番茄 | 进度环重绘 + 阶段色；关联任务名常驻顶部；控制按钮层级重排（开始为主按钮，重置/跳次级化） |
| 复习 | 改为单卡沉浸刷题模式（见 §6）；顶部进度条 x/y |
| 卡片 | 科目筛选 Chip 行 + 分组视图（见 §4）；网格卡片展示首图 |
| 统计 | 指标卡 + 图表区（见 §8）；图表配色取科目色 |
| 设置 | 分组卡片化（番茄钟/提示音/外观/科目/数据），行高与间距统一 |

---

## 4. 卡片知识管理升级（本期重点）

### 4.1 分科目管理

**信息架构**：卡片页从「单一网格」升级为「科目维度组织」。

- **顶部科目筛选行**（横向 Chip，可滚动）：
  `全部(52) | 数据结构(18) · 计组(12) · 操作系统(9) · 计网(6) · 数学(4) · 英语(2) · 政治(1)`
  - 每个 Chip 带科目标色圆点与数量；选中态用科目色填充。
- **两种浏览模式**（右上角切换，记忆用户偏好）：
  1. **网格模式**（现状）：按筛选后的科目平铺；
  2. **分组模式**：按科目分组，sticky header（科目色条 + 名称 + 数量）。
- **科目待复习角标**：Chip 上叠加今日到期数（松绿小徽标），与复习页联动。
- **撰写页科目前置**：新建时科目选择置于表单首行（大号色块选择器），引导「先选科目再写卡」。

### 4.2 专属卡片撰写页（CardEditScreen）

以**全屏编辑页**替代现 BottomSheet（BottomSheet 空间局促、无法承载图片管理）：

**布局（平板横屏双栏，竖屏单栏滚动）**

```
┌──────────────────────────────────────────────────────────────┐
│  ← 返回     新建卡片 / 编辑卡片                      [保存]   │
├──────────────────────────────┬───────────────────────────────┤
│  [科目标色块选择器 · 必选]     │                               │
│  类型(错题/知识点) 来源 标签   │      实时预览（卡片成品态，     │
│  ────────────────────        │      可点按模拟翻转）           │
│  正面（问题/知识点）           │                               │
│  ┌──────────────────────┐    │                               │
│  │ 多行输入              │    │                               │
│  └──────────────────────┘    │                               │
│  背面（答案/解析）             │                               │
│  ┌──────────────────────┐    │                               │
│  │ 多行输入              │    │                               │
│  └──────────────────────┘    │                               │
│  图片附件 [拍照] [选图]        │                               │
│  ┌────┐ ┌────┐ ┌────┐        │                               │
│  │ IMG│× │ IMG│× │ IMG│×       │  （×删除，长按拖拽排序）        │
│  └────┘ └────┘ └────┘        │                               │
└──────────────────────────────┴───────────────────────────────┘
```

**功能点**

- 新建 / 编辑复用同一页面（路由带 `cardId`，编辑态载入已有数据与图片）。
- **图片管理**：拍照、选图、单张删除、**长按拖拽排序**、点击**全屏查看器**（双指缩放 + 双击放大，`transformable` 自研，不引新依赖）。
- **保存时图片压缩**：最长边 1600px、JPEG 质量 85（控制存储占用，Coil 加载更快）。
- **实时预览**：右侧实时渲染卡片成品（含科目标签、类型、图片缩略），可点按模拟翻转。
- 未保存离开（返回）弹确认（有内容变更时）。
- **卡片详情页（CardDetailScreen）**：点击网格卡片进入——完整正反面 + 大图浏览 + 复习历史 + 编辑入口（替代现「点击原地翻转」；原地翻转保留为轻量交互）。

### 4.3 数据模型变更（DB v1 → v2）

| 变更 | 内容 |
|---|---|
| `KnowledgeCard` 增列 | `tags: List<String>`（Converter 存 JSON 文本）、`updatedAt: Long` |
| 新表 `ReviewRecord` | `id, cardId(FK CASCADE), result(FORGET/VAGUE/REMEMBER), reviewedAt` —— 复习历史，支撑详情页与统计 |
| 迁移 | `exportSchema = true`（导出 schema JSON 入库）+ 手写 `Migration(1,2)` + 迁移测试 |

### 4.4 检索与筛选

- 顶部搜索框：对 `front / back / source / tags` 做 LIKE 检索（Room `%kw%`）。
- 组合筛选：类型（错题/知识点）× 掌握度（忘记/模糊/记得）× 科目。

### 4.5 验收标准

- [ ] 科目筛选行正确计数，筛选/分组视图切换正常。
- [ ] 全屏撰写页可新建与编辑；平板横屏双栏、竖屏单栏。
- [ ] 图片可拍照/选图/删除/排序/全屏缩放查看；保存后已压缩。
- [ ] 实时预览随输入刷新，可模拟翻转。
- [ ] 详情页展示复习历史；编辑入口正常。
- [ ] DB 从 v1 无损迁移到 v2（老数据保留）。

---

## 5. 番茄钟完善（功能闭环）

- **悬浮窗体验**：
  - 首次开启时检测 `Settings.canDrawOverlays()`，无权限则弹窗引导跳转系统授权页（华为路径：显示在其他应用上层）；
  - 悬浮窗样式按墨·纸重绘（纸卡底、hairline、圆角 12dp、等宽字体），支持**点击主体返回应用**、拖动、关闭；
  - 增加迷你气泡形态（只显示 mm:ss，点击展开完整控件）。
- **任务关联闭环**：
  - 看板卡片增加「▶ 开始番茄」按钮 → `TimerController.start(taskId)` 并跳转番茄页；
  - 番茄页顶部任务选择器（可换绑/清空）；
  - 完成专注时 `taskDao.incrementPomodoro()`（修问题 #2）；
  - 通知与悬浮窗显示关联任务名。
- **铃声系统生效**（修问题 #1）：
  - `ringtoneId` 映射三组内置音色（打包 `res/raw`：默认/轻柔/清脆，均为短促柔和音）；
  - `volume` 通过 `AudioAttributes + setVolume()` 生效；
  - `vibrationOnly=true` 时只震动不发声（保持现状）。
- **其他**：通知刷新降频至 1Hz（修问题 #10）；专注中可选「保持屏幕常亮」；专注时建议勿扰（可选开关）。

## 6. 复习系统升级

- **刷题模式**：默认改为单卡沉浸——正面 → 翻面 → 忘记/模糊/记得 → 下一张；顶部进度条 `x/y`；完成页小结「今日已复习 N 张，M 张需要重记」。
- **每日提醒**：WorkManager 每日定点通知「今日有 N 张卡片待复习」（时间可在设置调整，默认 21:00；到期数实时查询）。
- **due 跨天刷新**（修问题 #9）：`now` 改为随日期变化的 StateFlow 驱动查询。
- **复习历史**：每次评价写入 `ReviewRecord`，详情页可见「复习轨迹」。
- **间隔可调**（PRD 开放项转正）：设置中可切换「标准 1/2/4/7/15/30」或自定义档位。

## 7. 看板增强

### 7.1 日期导航：历史 / 未来任意日看板【用户明确需求，优先实施】

**现状问题**：`TaskDao.observeAll()` 无日期过滤，所有任务混排显示，标题固定「今日看板」——既回看不到历史某天做了什么，也无法预排未来任务。

**设计**：

- **日期导航条**（标题区下方）：

  ```
  │  ←   [ 8月28日 · 今天 ]   →  │        [回到今天]
  ```

  - 左右箭头逐日切换；**点击中间日期**弹出 Material3 `DatePicker`，跳转任意历史 / 未来日期；
  - 「今天」在导航条上高亮；非今日视图时右上出现「回到今天」快捷按钮。
- **视图语义**：标题随所选日期变化——过去「8月26日 · 回顾」、今天「今天 · 8月28日」、未来「8月30日 · 预排」；进度头部统计**所选日**的完成情况（x/y）。
- **数据层**：新增 `TaskDao.observeByDate(dayStart, dayEnd)`（按任务 `startTime` 落在所选日归属），ViewModel 暴露 `selectedDate: StateFlow<LocalDate>`；**数据模型无需变更**（`startTime` 已有）。
- **跨天任务**按开始日归属，卡片仍显示完整时间范围。
- **空日期**：空状态插画 +「这一天没有任务」/ 未来日「还没有安排，点右上角新建」。
- **动效**：切日时三列内容 crossfade + 8dp 位移（200ms），日期数字 `AnimatedContent` 滑动换字。
- 未来日期可建任务（预排）；历史日期任务只读不可改状态（避免篡改记录），卡片隐藏勾选框。

### 7.2 其他增强

- 任务**编辑**（复用新建弹窗，路由化）。
- **科目筛选/分组**（PRD F2 补全，交互与卡片页科目 Chip 一致）。
- 拖拽换列（长按拖起 + 换列动画），或降级为长按菜单改状态。
- 任务「预估番茄数」字段（PRD 开放项转正），卡片显示 `2/3 ●●○` 进度。
- 详细卡片三态视觉与动效见 `OPTIMIZATION-BOARD.md`。

## 8. 统计可视化（详细设计）【用户明确需求：告别单调指标卡】

**现状问题**：统计页只有 6 张平铺指标卡，无图表、无时间范围切换、无科目维度、无历史趋势。

### 8.1 页面布局（平板横屏）

```
┌──────────────────────────────────────────────────────────────┐
│  学习统计                    [日 | 周 | 月]                    │
├───────────────────────┬──────────────────────────────────────┤
│  今日专注              │   科目分布（环形图）                    │
│  4h 35m   ▁▃▅▆▇      │    ╭───╮  数据结构 40% · 1h50m       │
│  （大数字+迷你趋势）    │    ╰───╯  计组 25% · 操作系统 20% …  │
├───────────────────────┤                                      │
│  近 30 日专注柱状图     │   连续专注 · 日历热力图                 │
│  ▁▂▅▇▃▆█▂▁▅…         │    ▢▢▣▩▣▢▢ ▢▣▩██▣▢ …（12 周）        │
│  （今日柱高亮，可点看值）│    连续 7 天 · 累计 21 天              │
├───────────────────────┴──────────────────────────────────────┤
│  任务完成率（按科目条形）  数据结构 ████████░░ 80%              │
│                          计组     █████░░░░░ 50%              │
│                          [导出 CSV]   [生成周报]               │
└──────────────────────────────────────────────────────────────┘
```

### 8.2 图表清单

| # | 图表 | 形式 | 数据 | 交互 |
|---|---|---|---|---|
| V1 | 专注时长指标卡 | 大数字 + 7 日迷你趋势（sparkline） | 日/周/月汇总 | 随范围切换 |
| V2 | **每日专注柱状图** | Canvas 柱状，朱砂渐变，今日高亮 | 近 7/30 日每日专注分钟数 | 点击柱子浮出「x h y m」；范围切换时柱高插值动画 |
| V3 | **科目分布环形图** | Canvas 环形（科目标色），中心显示总时长 | 所选范围各科目专注占比 | 点击扇区弹出该科目时长；图例带 % 与时长 |
| V4 | **连续专注热力图** | 12 周日历格子（GitHub 打卡风格），色深=当日专注时长 | 每日专注分钟 → 5 档色阶（纸白→松绿） | 点击格子看当日数据；streak 数字大字展示 |
| V5 | **任务完成率条形** | 按科目横向条形（科目色） | 各科目 已完成/全部 | — |
| V6 | 番茄数 / 完成率 / streak 指标 | 保留三张卡但重设计 | 同现状 | 数字变化滚动动画 |

### 8.3 交互与动效

- **日/周/月分段切换**：`SegmentedButton`；切换时所有图表数据 **300ms 插值动画**（柱高、环角度、折线点平滑过渡），而非跳变。
- 图表入场：首次进入依次淡入 + 绘制展开（柱子从 0 长起、环从 0° 扫到目标角，各延迟 60ms）。
- 空数据：对应区块显示线性插画空状态，不显示断裂坐标轴。

### 8.4 技术方案

- 全部 **Compose Canvas 自绘**（零第三方依赖），封装 `ui/stats/charts/`：`BarChart` / `DonutChart` / `LineChart` / `HeatmapCalendar` 四个可复用组件（进度环组件与看板进度头部共用）。
- 配色全部取自设计令牌与科目标色，深浅色模式各校准一版。
- **前置依赖**：`PomodoroSession` 增加 `subjectId`（记录时从关联任务快照，修问题 #11），随 DB v2 迁移；`Task` 已有 `startTime/isCompleted` 可直接支撑 V5。
- 数据导出 CSV（SAF）+ 周报文本（本周专注 X 小时、完成 Y 番茄、复习 Z 卡…）系统分享。

## 9. 数据与工程质量

- **备份升级**：导出改为 **ZIP**（`backup.json` + `images/` 目录），导入对应解包——根治问题 #4；保留纯 JSON 导出（无图模式）；每周提醒备份（本地通知，可选）。
- **DB 规范**：`exportSchema=true`，schema JSON 入 git；v1→v2 迁移测试。
- **单元测试**：`ReviewLogic`（间隔推进/回退/重置）、`TimerController`（暂停恢复精度、阶段切换）、`BackupManager`（导出→导入 round-trip）。
- **图片治理**：删除卡片时同步删文件（已做）；启动时低频清理孤儿图片文件；Coil 请求显式尺寸约束。
- **权限流程**：首启申请 `POST_NOTIFICATIONS`（修问题 #5）。

## 10. 优先级与里程碑

| 优先级 | 内容 |
|---|---|
| **P0** | §4 卡片升级全量（分科目 + 撰写页 + 图片管理 + DB v2）；§3.1–3.2 字体/Insets/自适应 |
| **P1** | §7.1 **看板日期导航**（用户明确需求：历史/未来任意日看板）；§5 番茄钟闭环（悬浮窗引导/任务关联/铃声生效）；§6 刷题模式 + 每日提醒；§7.2 看板编辑/筛选；§9 备份 ZIP + 权限流程；看板三态与动效（`OPTIMIZATION-BOARD.md`） |
| **P2** | §8 **统计可视化 V1–V6**（用户明确需求：柱状图/环形图/热力图/完成率）；§3.3 动效打磨；单元测试补齐 |

| 里程碑 | 内容 | 交付标准 |
|---|---|---|
| **M1（v1.1）** | 卡片升级 + UI 基础升级 | 分科目浏览、撰写页可编辑可管图、DB v2 迁移无损、字体/insets 落地 |
| **M2（v1.2）** | 番茄钟闭环 + 复习升级 + 看板增强 | 技术债 #1/2/3/5/6/9/12 清零 |
| **M3（v1.3）** | 统计图表 + 备份 ZIP + 打磨 | 技术债 #4/11 清零，图表可用，核心逻辑有测试 |

## 11. 本期不做

- 云同步 / 多设备实时同步（延续 PRD 决策，导出导入已覆盖迁移场景）。
- 卡片富文本编辑、LaTeX 公式、OCR 拍图取字（记录为远期想法）。
- 桌面小组件（Widget）。

---

## 附：技术债清零对照

| 债 | 修复版本 | 对应章节 |
|---|---|---|
| #1 铃声未生效 | M2 | §5 |
| #2 pomodoroCount 未累加 | M2 | §5 |
| #3 任务关联缺失 | M2 | §5 |
| #4 备份不含图片 | M3 | §9 |
| #5 通知权限 | M2 | §9 |
| #6 悬浮窗引导/样式 | M2 | §5 |
| #7 Insets | M1 | §3.2 |
| #8 字体 | M1 | §3.1 |
| #9 复习跨天 | M2 | §6 |
| #10 通知 5Hz | M2 | §5 |
| #11 Session 无科目 | M3 | §8 |
| #12 看板编辑/筛选 | M2 | §7 |
| #13 卡片编辑/图片管理 | M1 | §4 |
