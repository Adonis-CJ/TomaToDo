# 看板 UI 与动效专项优化方案

> 针对 v1.0 看板（`BoardScreen.kt`）的专项设计。关联：`OPTIMIZATION.md`（总方案）、`PRD.md` §6.5。
> 状态：待评审 → 通过后随 M1 实施。

---

## 1. 现状问题诊断

对照当前实现逐条检查：

| # | 问题 | 现状表现 |
|---|---|---|
| 1 | **完成态零视觉差异** | 已完成卡片与待办卡片外观完全相同，仅「列位置」变了——扫一眼看不出哪些做完了 |
| 2 | **状态切换交互隐蔽** | 点击整张卡片在 待办→进行中→完成→待办 循环切换：不可发现、易误触（想点删除结果切状态）、无法直接「完成」 |
| 3 | **无全局进度感知** | 顶部只有日期 + 新建按钮，今天完成率一眼看不出 |
| 4 | **卡片层次平淡** | 科目标签只是「小圆点+灰字」；时间与内容无层级；三列除标题外无身份色 |
| 5 | **删除按钮裸露** | 每张卡右上角常驻垃圾桶图标，视觉噪音大且易误触 |
| 6 | **零动效** | 状态变化是「瞬移」：卡片从一列消失、在另一列出现，无过渡；无入场/删除动画 |
| 7 | **无日期维度** | `observeAll()` 无日期过滤，历史任务与未来任务混排显示，标题固定「今日看板」——回看与预排都不可能 |

---

## 2. 完成状态可视化设计

### 2.1 三态视觉语言（核心）

一张卡片的三种状态，用**四个通道**同时表达（色、形、字、透明度），任何一通道单独看都能分辨：

| 通道 | 待办 TODO | 进行中 DOING | 已完成 DONE |
|---|---|---|---|
| 左侧状态色条（4dp） | 灰墨 `Line` | **朱砂** `accent` | 松绿 `success` |
| 圆形勾选框（卡左下） | 空心圆（灰描边） | 半填充 + 内点（朱砂） | **实心圆 + 白勾**（松绿） |
| 任务内容文字 | 正常墨色 | 正常墨色 + 加粗 | **划线** + 灰墨 |
| 卡片整体 | 纸白 100% | 纸白 100% + 朱砂 hairline 描边 | 透明度降至 **55%** |

> 设计逻辑：完成 = 「变淡 + 划线 + 变绿」，符合「已翻篇」的心理模型；进行中 = 朱砂强调（与应用主行动色一致）。

### 2.2 卡片结构（优化后）

```
┌─────────────────────────────┐
│▌[数据结构]            09:00 │   ← ▌=状态色条(贴卡左缘,4dp,通高)
│▌                            │
│▌ 数据结构刷真题 · 2017年卷    │   ← 内容：完成后划线+变灰
│▌                            │
│▌ ◯ 09:00–11:00    ▶ ⋯      │   ← ◯=三态勾选框  ▶=开始番茄  ⋯=更多(删除收进来)
└─────────────────────────────┘
   进行中态：hairline 朱砂描边；完成态：整体 55% 透明度
```

- 科目标签升级为**色块 chip**：科目色 12% 透明度底 + 科目色文字（替代小圆点+灰字）。
- 时间改等宽数字（IBM Plex Mono），主时间右上一行、时间范围左下一行，层级拉开。
- 删除收进「⋯」菜单（或滑动删除），卡片表面不再有常驻危险按钮。

### 2.3 今日进度头部（全局感知）

标题区增加当日完成进度，一眼看清「今天过得怎么样」：

```
┌────────────────────────────────────────────────────────┐
│ 今日看板                    ┌────┐                      │
│ 8月28日 · 3/6 已完成         │ 50%│  ← 环形进度(朱砂→松绿) │
│ ▰▰▰▰▰▱▱▱▱▱                 └────┘      [＋ 新建任务]     │
└────────────────────────────────────────────────────────┘
```

- 左：日期 + `x/y 已完成` 文案 + 细线性进度条；
- 右：环形进度（完成后从朱砂渐变为松绿）；
- 数字变化用滚动/淡入动画（见 §4）。

### 2.4 列头强化

```
│ ● 待办 2 │    │ ◐ 进行中 1 │    │ ✓ 已完成 3 │
│   (灰墨)  │    │  (朱砂)    │    │  (松绿)    │
```

列头图标与列身份色一致（三列各有状态色 6% 透明度底色），空列显示线性 SVG 空状态插画 + 引导文案（「把今天的第一个任务放进这里」）。

### 2.5 日期导航：历史 / 未来任意日看板【用户明确需求】

现状只能看到混杂的全部任务。优化后标题区下方增加**日期导航条**：

```
┌────────────────────────────────────────────────────────────┐
│ 今天 · 8月28日                                    [回到今天] │
│ ┌─────┬──────────────────────────┬─────┐                   │
│ │  ←  │   8月28日 · 今天           │  →  │   [＋ 新建任务]    │
│ └─────┴──────────────────────────┴─────┘                   │
│ ▰▰▰▰▰▱▱▱▱▱  3/6 已完成                                     │
└────────────────────────────────────────────────────────────┘
```

- **← / →** 逐日切换；**点击中间日期**弹 Material3 `DatePicker` 跳转任意历史/未来日期。
- 视图语义：历史「8月26日 · 回顾」，今天「今天 · 8月28日」，未来「8月30日 · 预排」；进度头部统计**所选日**的 x/y。
- 非今日视图右上角显示「回到今天」快捷按钮。
- **历史日期只读**：卡片隐藏勾选框与状态切换（不篡改记录）；未来日期可正常新建任务（预排）。
- 数据层：`TaskDao.observeByDate(dayStart, dayEnd)`（按 `startTime` 归属日），`selectedDate: StateFlow<LocalDate>`；数据模型无需变更。
- 空日期：空状态插画（「这一天没有任务」/ 未来日「还没有安排」）。

---

## 3. 交互重设计

| 操作 | 现状 | 优化后 |
|---|---|---|
| 切换状态 | 点整卡循环三态 | **点击勾选框**：待办→完成（直达）；勾选框/状态色条为唯一状态入口 |
| 打开/编辑 | 无 | **点卡片主体** → 任务编辑页（配合 OPTIMIZATION §7） |
| 删除 | 常驻垃圾桶 | **左滑删除**（SwipeToDismissBox）+ 底部「已删除 · 撤销」Snackbar，5s 可撤销 |
| 开始番茄 | 无 | 卡片「▶」按钮 → 关联任务启动番茄钟（OPTIMIZATION §5 任务闭环） |
| 拖拽换列 | 无 | P1：长按拖起跨列（先做勾选框直达完成，拖拽后补） |

> 原则：**状态变更必须可预期、可直达**。「完成」是最高频动作，一次点击直达，不再循环。

---

## 4. 动效方案

### 4.1 动效规格表

统一曲线：标准过渡用 `FastOutSlowInEasing`；强调/弹性用 `spring(dampingRatio = 0.6f)`。全局动效时长带 **150–350ms**，克制不花哨（墨·纸原则）。

| # | 动效点 | 规格 | 说明 |
|---|---|---|---|
| A1 | **勾选完成** | 勾笔画 Path 描边动画 220ms + 实心圆 scale 0→1（spring） | 白勾从起点「画」到终点，核心满足感动效 |
| A2 | **完成划线** | 内容文字上划线从左到右展开 240ms（Canvas 遮罩） | 与勾选同步；文字同时降透明度 |
| A3 | **卡片完成弹跳** | scale 1→0.97→1（spring，120ms） | 轻微「按了一下」的实体感 |
| A4 | **跨列移动** | 原列 `fadeOut+缩小(150ms)`，新列 `fadeIn+上浮8dp(200ms, 30ms stagger)` | LazyColumn `animateItem` 的 fade 规格 |
| A5 | **进度头部** | 进度条/环 `animateFloatAsState` 300ms；数字 `AnimatedContent` 上滑换字 | 完成第 N 个任务时环从朱砂向松绿过渡 |
| A6 | **新建入场** | 新卡 fadeIn + 从底部 16dp 上浮（200ms） | ModalBottomSheet 保存后触发 |
| A7 | **滑动删除** | 跟手位移 + 松手滑出（200ms）+ 高度折叠（150ms） | 配「撤销」Snackbar |
| A8 | **列表首屏入场** | 各卡片 stagger 30ms 依次 fadeIn+上浮 | 仅首屏一次，滚动不再触发 |
| A9 | **勾选框状态切换** | 三态图形 `AnimatedContent` crossfade 120ms + spring 缩放 | 待办↔进行中↔完成的框体形变 |
| A10 | **触感** | 完成任务 `HapticFeedback.LongPress`；勾选切换 `TextHandleMove` | 遵守「静音+仅震动」全局设置 |
| A11 | **切日过渡** | 三列内容 crossfade + 8dp 位移（200ms）；日期数字 `AnimatedContent` 水平滑动换字（方向跟随 ←/→） | 消除切日跳变； DatePicker 弹出为系统动效 |

### 4.2 完成时刻的编排（A1+A2+A3+A5 连携）

```
点击勾选框
  ├─ 0ms    勾选框变松绿实心，白勾描边画出（A1, 220ms）
  ├─ 0ms    卡片内容开始划线（A2, 240ms）+ 整卡降透明度至 55%
  ├─ 0ms    卡片轻微弹跳（A3）
  ├─ 60ms   卡片从「进行中/待办」列淡出（A4）
  └─ 120ms  在「已完成」列淡入上浮；顶部进度环 +1 跳动（A5）
```

全部动效在 ~350ms 内完成，干净利落，无庆祝彩带类噪音（符合克制定位）。

---

## 5. 实施清单

| 文件 | 改动 |
|---|---|
| `ui/board/BoardScreen.kt` | 重构：列身份色、进度头部、**日期导航条 + DatePicker**、卡片三态渲染、勾选框交互、滑动删除、空列插画 |
| `ui/board/TaskCard.kt`（新拆） | 卡片组件独立：状态色条 / 勾选框（自绘 Canvas）/ 划线动画 / chip 标签；历史日只读态 |
| `ui/board/BoardAnimations.kt`（新） | A1–A11 动效封装（`rememberProgressAnim`、`drawCheckPath` 等可复用件） |
| `ui/board/TaskViewModel.kt` | 增加 `selectedDate: StateFlow<LocalDate>` 与 `setStatus(task, status)`；删除配合撤销（软删/延迟删） |
| `data/db/TaskDao.kt` | 新增 `observeByDate(dayStart, dayEnd)` 按日过滤查询 |
| `ui/components/`（新） | 空状态插画组件、环形进度组件（统计页复用） |

## 6. 验收标准

- [ ] 已完成卡片有划线 + 变灰 + 变绿三重区分，扫一眼可分清三态。
- [ ] 一次点击勾选框即直达「完成」；删除为滑动 + 可撤销。
- [ ] 顶部可见当日完成进度（x/y + 环），随完成实时动画更新。
- [ ] 可通过 ←/→ 或 DatePicker 跳转任意历史/未来日期；历史只读、未来可预排；「回到今天」一键返回。
- [ ] §4 全部动效落地（含 A11 切日过渡），完成时刻编排流畅无跳变。
- [ ] 全程无 emoji，图标均为 SVG/ImageVector；动效时长符合规格表。
